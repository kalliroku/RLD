# 퀴니(Quinnie / Q-kun) 흉상 — design 산출물

Q-Learning 의인화 세르파, 플레이어의 **첫 동료**. 신규 절차 도트 **흉상**.
정식 브리프: `docs/PM/handoffs/2026-06-06-quinnie-portrait-to-claude-design.md`.
소유권 규칙: `design/README.md` — 이 폴더는 Claude Design 작성, 이식은 Claude Code.

## 파일

| 파일 | 용도 |
|---|---|
| `drawQkunPortrait.js` | 산출물 본체. `export function drawQkunPortrait(ctx, cx, baseY, scale = 6)` |
| `Quinnie Portrait Preview.html` | 소용량 standalone 미리보기. **실제 형제 흉상 `drawRepliPortrait`·`drawRikaPortrait` 를 verbatim 복사**해 셋을 나란히 렌더 + eyeline 가이드. raster 임베드 없음 |

## 드롭인 호환 — 형제 흉상(레플리/리카)과 동일 골격 ★

father(scale=4, 전신)는 기준 부적합. **레플리·리카(scale=6, waist-up 흉상)** 에 맞춤:

- **시그니처**: `(ctx, cx, baseY, scale = 6)` — child/repli/rika 와 동일.
- **흉상(BUST)**: 다리·발 없음. **torso 바닥 = `y 0` = baseY**(허리 크롭). baseY 는 발밑이 아니라 흉상 하단선. *(브리프 §4 는 "발밑 기준"이라 적었으나 실제 형제 코드는 모두 허리 크롭 흉상 — 형제 코드 기준으로 맞춤.)*
- **표준 골격**(형제와 동일 좌표대): torso `-19..0`(폭 ~12) · neck `-21..-19` · head `-33..-21`(10×12) · **눈높이 `y=-29`** · 코 `~-26` · 입 `~-24` · 머리/모자/귀는 `-33` 위.
- **광원**: +x 횃불 측 lit / −x shadow. 광원·페이드·딤은 호출부(`_drawGuildScene`) 소유.
- **팔레트**: `R(x,y,w,h,c)` fillRect + **함수 상단 로컬 색 토큰**(형제 방식 — repli 하늘색/금발, rika 분홍처럼 캐릭터색은 로컬 hex). raster 이미지/임베드 색 신규 도입 없음.
- **검증**: 미리보기의 `eyeline` 토글로 셋의 눈높이가 `y=-29` 한 줄에 맞음을 확인.

## 디자인 의도 (브리프 §3·§6 준수)

- **갈색 강아지 수인 여성, 인간형 본체**: 종족 신호 = 머리 위 갈색 **개 귀**(최강 신호) + **꼬리끝**이 허리 크롭 아래 모서리로 살짝. 코끝 점 1개.
- **동안 + 글래머 + 무도가**: 둥근 동안 골격 위에 모래시계(어깨 넓고 허리 tuck) + 절제된 bust 셰이딩(노출 X). 무도가 = 크림 **무도복(gi)** + 갈색 가죽 어깨 가드 + **rust 새시**(그녀의 따뜻한 시그니처색).
- **밝은 덜렁이 + 충성·따뜻함**: 크고 밝은 **호박색 눈** + 큰 캐치라이트 + 입꼬리 올라간 **활짝 열린 미소 + 덧니** + 은은한 홍조. **한쪽 귀를 접어** "덜렁이" 표현.
- **톤 가드**: RLD 어두운 던전 채도와 어우러지게 — 고채도 금색 X(브래스는 새시 매듭쇠 1점). 크림 gi 는 rika 흰 티와 같은 밝기대라 무대에서 튀지 않음. 레플리(하늘색)·리카(데님) 사이에서 **따뜻한 갈색/크림/rust** 로 색 대비 확보.

## 이식 참고 (Claude Code)

- **추가 위치**: `web/js/game/opening-art.js` 에 `drawQkunPortrait` export 추가(레플리/리카 옆). 시그니처·골격 동일하므로 그대로 드롭인.
- **무대 배선**: `main.js:_drawGuildScene` 의 firstclear 비트에서 레플리/리카와 동일 호출 패턴으로 퀴니 슬롯 배치(같은 baseY 라인·scale·광원).
- **미리보기 이식(권장, §5-2)**: `web/_npc-preview.html` 에 퀴니 슬롯 추가 — web/ 영역이므로 여기선 손대지 않음. 이 폴더 미리보기가 그 레이아웃의 참조.
- 색 토큰은 함수 상단에 모여 있으니 bm 레퍼런스 확정 후 톤 미세조정은 토큰만 만지면 됨.

## 열린 것 (브리프 §7 — bm 입력 대기)

- **컨셉 레퍼런스 미확정**: 현재는 브리프 §3(히타무 키얀 "느낌" 오리지널) 기준 **1차 제안**. father·레플리·리카처럼 bm 레퍼런스 확정 → 피드백 루프로 톤 미세조정 예정.
- 조정 후보: 귀 모양/접힘 정도, 표정 강도(현재 활짝 → 차분), 무도복 색조(크림 ↔ 더 따뜻한 탄), 어깨 가드 유무.
