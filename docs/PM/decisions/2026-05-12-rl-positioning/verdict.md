# Verdict — D-2026-05-12-4 RL 포지셔닝

**판정**: **C — 제3의 길**. RL 은 양념 (B 의 골격) + 수동 비용만 소폭 보정 (A 의 수술 1 부분 채택). 하이퍼파라미터 슬라이더와 가차는 둘 다 컷.

**근거**:
- 시뮬레이터의 100배 격차 (HybridPlayer 29.5/31) 는 측정값이고, A 의 "수술하면 1배로 무너진다"는 검증되지 않은 가정. 데이터에 충실한 해석은 B.
- 솔로 개발자 6개월 일정에서 B 는 매 스텝이 출시 가능한 부분 결과 (PCG/RunState 재배선) 인 반면 A 는 수술 → 재검증 → 마스터리 시스템 신규 구축의 4단 사슬이 모두 완성돼야 코어 동사가 작동.
- 단 B 그대로 두면 수동이 너무 강해진다. 수동 스텝당 식량 1→2 만 채택해 후반 마스터리 곡선에 AI 의미를 남긴다 (`run-state.js:225` 한 줄 변경, 시뮬레이터로 즉시 검증 가능).
- 알고리즘 다양성은 *숨기는 것*이 아니라 *성격 태그로 1차 라벨* + 학명은 툴팁. Q-value 히트맵·sparkline 은 양념의 시각적 시그니처로 디폴트 ON 유지.

**적용**:
- 즉시: B-001 정체성 카피 "알고리즘이 곧 캐릭터인 시드 기반 로그라이크" 로 확정. B-002 가차 섹션 GDD §3.3.4 삭제.
- 단기: T2B-1/2/3 (시드 챌린지 + 모디파이어 + 알고리즘=캐릭터 격하) P1 승격. 신규 B-108 (수동 식량 1→2), B-109 (모디파이어 추가 후 HybridPlayer 회귀 검증), B-110 (Daily Seed UI 프로토타입).
- Tier 2: 옵션 B 전부 채택 + 옵션 A 의 T2A-1 만 *축소된 형태로* 흡수 (수동만, AI 미수정).

**컷되는 항목**: T2A-2 하이퍼파라미터 슬라이더 + 리더보드 영구 폐기. T2A-3 시뮬레이터 재검증 작업 (목적 자체가 사라짐). GDD §3.3.4 NPC 가차 시스템 (잔존하면 안 됨).

**한 줄 정체성**: RLD 는 **알고리즘이 곧 캐릭터인 시드 기반 로그라이크** 다. RL 교육은 부산물.
