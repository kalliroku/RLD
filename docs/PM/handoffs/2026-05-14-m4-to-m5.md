# 인계 — M4 마감 → M5 (1.0 폴리시 + 한영 i18n + 출시 페이지)

| 항목 | 값 |
|---|---|
| 인수일 | 2026-05-14 |
| 인계자 | 자율 세션 (D-12 closure + 모바일 트랙 + 사망 페널티 + 모디파이어 12종) |
| 인계 대상 | 다음 작업 에이전트 (M5 진입 — BGM 4트랙 + 한영 i18n + 출시 페이지 + 잔여 P2 fast-follow) |
| 예상 작업 기간 | M5 전체 ~6~7개월 (VISION §5). 다음 1주 권장: B-207 (silent_q placeholder) 또는 i18n 텍스트 키 분리 착수 |
| 진입 후 첫 행동 | §5.1 — `STATUS.md` + `BACKLOG.md` + `VISION.md` + 이 문서 읽고 M5 첫 작업 선택 |

---

## 0. 한 줄 정체성 (불변)

> **알고리즘이 곧 캐릭터인 시드 기반 로그라이크 — 매 런 다른 모디파이어 아래 한정된 세르파 풀로 던전을 답파한다. RL 교육은 부산물.**

D-2026-05-12-4 verdict 그대로. M4 마감으로 출시 1.0 (M5) 의 모든 콘텐츠/기능 영역 정렬 완료. 남은 것은 폴리시 (BGM + i18n + 출시 페이지).

---

## 1. M4 마감 스냅샷

### 완료된 산출물 (이번 세션 8 커밋)

| Commit | 마감 항목 |
|---|---|
| `9179639` | D-12 closure — 알고리즘 17 파일 × 33 인스턴스 deflection 패치 (intendedKey/actualKey 분리) + sim grid.modifierSet 활성화 |
| `4136c79` | D-12 review warnings fix — STATUS 헤더 / getGrid 캐시 가드 / B-205 BACKLOG 분리 |
| `9083815` | B-204 모바일 핵심 스탯 3개 (HP/Gold/Steps) + localStorage 박제 토글 |
| `acaccfd` | B-201 모바일 fit-to-screen + 미니맵 (≥25 grid) + TilemapRenderer onAfterRender hook |
| `32a83ca` | B-202 모바일 하단 탭 바 (5 anchor scroll) |
| `7c632b5` | B-203 사망 페널티 — DEATH_LIMIT = 4 + resetForDeathLimit + guild header 카운터 |
| `011df10` | B-206 save migration toast — 기존 deathCount ≥ 4 로드 시 1회 안내 |
| `a1c62cd` | M4 모디파이어 12종 — 환경 6 + 제약 6 (데일리 전용) |

### 측정 결과

#### D-12 closure (20-run HybridPlayer)

| Modifier | B-109 (manual approx only) | D-12 closure 후 (algorithm-side) | Δ |
|---|---|---|---|
| off | 24.5/27 ✓ | **25.6/27 ✓** | +1.1 |
| slippery | 24.4/27 ✓ | 14.0/27 | -10.4 |
| heavy_fog | 24.3/27 ✓ | 25.6/27 ✓ | +1.3 |
| two_only | 13.3/27 | 16.3/27 | +3.0 |
| 3종 합산 | 17.1/27 | 15.2/27 | -1.9 |

이중 페널티 가설 기각 (algorithm-only 14.6 vs 이중 14.0, +0.6 차이) — slippery 영향은 algorithm 측 deflection 자체.

#### 모디파이어 12종 캠페인 무영향 (20-run HybridPlayer modifier-off)

3회 baseline: 25.1 / 25.1 / 25.2 (평균 ~25.1, 모두 [23.5, 27.5] hit). modifier-off 무영향 확인.

### 비-자명 결정 (DECISIONS.md 박제)

- **D-2026-05-14-13** ✅ D-12 closure — Key 재생성 방식 채택 (사용자 선택, 최소 변경). 17 파일 일괄 패치 + sim grid.modifierSet 활성화. slippery 30% 의 algorithm 측 영향 -10.4 가 진실로 드러남.
- **D-2026-05-14-14** ✅ 모디파이어 12종 — 환경 6 + 제약 6, 데일리 전용 유지. 초기 명세 food_drain / gold_dry 는 데일리 이코노미 격리 (D-8) 로 효과 0 → mirror_input / damage_boost 로 교체. **B-205 자연 closure** (데일리 전용 유지로 slippery 30% 캠페인 위협 무 — M5 캠페인 modifier 도입 시 부활). 상호작용 룰: 환경 데미지 (poison_floor / acid_rain) 는 C-5 defense contract 회복 대상 외.

---

## 2. 코드 진입점 맵 (M4 마감 후)

| 영역 | 파일 | 메모 |
|---|---|---|
| 모디파이어 | `web/js/game/modifiers.js` | MODIFIERS 12종 + ModifierSet 메소드 12개 (resolveMovement / visibilityRange / shouldSkipTurn / poisonStepDamage / acidRainDamage / clampMaxHp / mirrorInput / healDisabled / damageMultiplier / visualizationMuted / pickCharacterPool + helper). PRNG salts (MOVEMENT / PICK / CHARS / WIND) per-effect 분리 |
| Agent | `web/js/game/agent.js move()` | TRAP/MONSTER damageMultiplier × Math.round (modifier-off 정수 등가). HEAL healDisabled 가드. `_resolveAction` 의 `this.modifierSet || grid.modifierSet` fallback (D-12 closure 유지) |
| 알고리즘 (17개) | `web/js/game/{qlearning,sarsa,...}.js` | episode loop: intendedPos/intendedKey/intendedTile (hide) + actualKey (track/restore). agent.move() 직후 `actualKey = ${agent.x},${agent.y}` 재생성 |
| Main 흐름 | `web/js/main.js handleAction` | wind_gust skipTurn + mirror_input → agent.move → poison/acid 후처리. agent 생성 시 hp_cap_50 maxHp clamp. `_syncAgentModifiers` 가 silent_q 시 showQValues=false + sparkline-wrap hide (W1 fix 로 userQPref 복원) |
| Run State | `web/js/game/run-state.js` | DEATH_LIMIT = 4 export. recordDeath() boolean 반환. resetForDeathLimit() — fresh playthrough (NG+ 와 구분) |
| Sim 통합 | `sim/simulator.js` | getGrid 가 modifierSet 부착 + manualPlayDungeon 의 `SLIPPERY_MANUAL_SUCCESS_MULT=0.85` 유지 (사용자 입력 오류 모델). hot-swap 가드 주석 (W2) |
| UI — 모바일 | `web/css/style.css @media (max-width: 700px)` | fit canvas + stats toggle + bottom tab bar (fixed, body padding-bottom 60px). 데스크탑 무영향 |
| UI — minimap | `web/js/main.js _renderMinimap` + `tilemap-renderer.js onAfterRender` hook | grid ≥25 일 때만 active. 매 frame redraw, fast-path silent skip |
| UI — death | guild header `#guild-deaths` + `.guild-res-warn` (deathCount ≥ LIMIT-1 시 빨강) | `_updateGuildResources` 갱신 |

---

## 3. M5 진입 전 정리 필요 항목

### 3.1 B-207 silent_q placeholder UI (P2, 0.5일)

silent_q modifier 활성 시 Q-heatmap 자리가 그냥 사라짐 → 사용자가 "RL 양념 자체가 사라졌다" 오해 가능 (D-4 정체성 보호). placeholder 한 줄 ("침묵의 학습 — 시각화 차단됨") 표시 권장. M4 modifier 12 리뷰 I1.

### 3.2 B-205 closure 후속 (M5 캠페인 modifier 도입 시 부활)

modifier 데일리 전용 유지로 자연 closure. M5 에서 캠페인 modifier 도입 결정 시 다음 재산정 필요:
- slippery 30% 강도 (D-10) → 15% 하향 / dungeon 화이트리스트
- 모든 12종의 캠페인 도입 적격성 (movement 영향 = slippery / wind_gust / mirror_input 위험, 나머지 보수적)

### 3.3 캠페인 발란스 게이트 정합

`HybridPlayer modifier-off` baseline 의 5×20 historical (24.6 / 25.6 / 24.9 / 25.4 / 24.1) + D-12 closure 후 (25.6) + M4 modifier 후 (25.1 / 25.1 / 25.2) = 평균 ~25.1, 모두 [23.5, 27.5] hit. M5 변경 (i18n 텍스트 분리 / BGM 추가 / 출시 페이지) 은 sim 무영향이라 게이트 재측정 필요 없음.

---

## 4. M5 우선순위 제안 (VISION §5 + BACKLOG P2)

| ID | 작업 | 출처 | 공수 |
|---|---|---|---|
| **B-207** | silent_q placeholder UI | M4 modifier I1 | 0.5일 |
| **신규** | BGM 4트랙 (Ch.1~3 / 데일리 / 전투-탐험) — 음원 결정 + 통합 | VISION §3 M5 | 5~10일 (음원 외주 의존) |
| **신규** | 한영 i18n 텍스트 키 분리 + 영어 카피 작성 | VISION §3 M5 | 7~14일 |
| **신규** | 출시 페이지 — github pages `index.html` + itch.io 페이지 폼 | VISION §1 / §5 M5 | 2~3일 |
| (옵션) | 자체 플레이테스트 5회 (M5 → 1.0 게이트) | VISION §8 | 분산 |

### 제안 순서

1. **B-207** (0.5일, 자율 가능) — silent_q UX 보강. 마무리.
2. **i18n 텍스트 키 분리** (자율 가능, 사용자 검수 필요) — UI 변경 없이 텍스트 추출. 영어 카피는 사용자 검수.
3. **출시 페이지 prep** (자율 가능) — Claude Code 가 직접 HTML/CSS 작성 (사용자 선호 — `feedback_design_tooling_preference.md` 메모리 참조)
4. **BGM 4트랙** (사용자 결정 필수) — 외주 / freesound / 자체 작곡 중 결정 필요. 트랙 결정 후 통합은 자율.

---

## 5. 시작 명령 (다음 에이전트 첫 행동)

### 5.1 컨텍스트 로드 (병렬 읽기)

1. 이 문서를 끝까지 읽음
2. 다음 5개 동시 읽기:
   - [`./2026-05-13-b109-to-m4.md`](./2026-05-13-b109-to-m4.md) — 직전 인계 (M3 → M4)
   - [`./2026-05-12-t2b2-to-b109.md`](./2026-05-12-t2b2-to-b109.md) — 모디파이어 시스템 원본 명세
   - [`../STATUS.md`](../STATUS.md) — 현재 상태 (M4 마감 후)
   - [`../BACKLOG.md`](../BACKLOG.md) — M5 잔여 + 신규 후속
   - [`../VISION.md`](../VISION.md) — 출시 1.0 그림 / M5 게이트

### 5.2 첫 작업 선택

§4 표를 보고 우선순위 확인. 자율 결정 가능한 범위:
- **B-207** silent_q placeholder — 자율 진입 (P2, 0.5일, 위험도 0)
- **i18n 텍스트 추출** — 자율 진입 가능, 영어 카피만 사용자 검수
- **출시 페이지 HTML** — 자율 진입 가능 (Claude Code 직접 작성)
- **BGM** — 사용자 결정 필요 (음원 출처)

### 5.3 절차

- 매 task 끝마다 PM 동기화 (`docs/PM/{STATUS,BACKLOG,DECISIONS}.md`)
- 커밋은 `/commit` 스킬로 (korean-git-reviewer 자동 호출, `[reviewed]` 마커)
- 세션 종료 시 `/handoff-save <slug>` 로 다음 세션 인계 작성

### 5.4 진행 중 막히면

- 자율 결정 가능: 본 문서 §4 + DECISIONS.md (D-2026-05-12-4, D-2026-05-14-13, D-2026-05-14-14) + VISION §5/§6 참조
- 자율 결정 불가: 사용자에게 묻기 (예: BGM 출처 / 영어 카피 검수 / 출시 페이지 한 줄 카피)
- 검증 실패: revert + 원인 분석 → 부분 보정 또는 BACKLOG item 신설

---

## 6. 이번 세션 변경 파일

### 코드

- `sim/simulator.js` — getGrid modifierSet 부착 + manualPlayDungeon 의 D-12 closure 주석 갱신 (W2 hot-swap 가드 주석 추가)
- `web/js/game/agent.js` — move() 의 TRAP/MONSTER/HEAL 처리에 modifierSet 가드 (damageMultiplier / healDisabled). D-12 closure 의 `actualKey` 패턴 (이 세션 변경 없음, 직전 세션에서 적용됨)
- `web/js/game/run-state.js` — DEATH_LIMIT export + recordDeath boolean 반환 + resetForDeathLimit 메소드 신규
- `web/js/game/modifiers.js` — MODIFIERS 12종 + ModifierSet 메소드 9개 + SALT_WIND PRNG salt
- `web/js/game/tilemap-renderer.js` — render() 끝에 `onAfterRender` hook (한 줄)
- `web/js/game/{17 algorithms}.js` — episode loop 패턴 일괄 변경 (`nextKey/originalTile → intendedKey/intendedTile + actualKey`)
- `web/js/main.js` — D-12 closure 후속 (intended/actual 분리), 모바일 트랙 (stats toggle / minimap / bottom tab bar), B-203 (deathLimitReached / guild deaths counter / startNewRun 분기), B-206 (save migration toast), M4 modifier 12 (handleAction 의 wind_gust + mirror_input + poison/acid + hp_cap_50 + silent_q sync)
- `web/index.html` — 모바일 markup (stats-toggle / minimap-wrap / bottom-tabs / char-section / dungeon-section), guild-deaths, stats-toggle data-target
- `web/css/style.css` — 모바일 미디어 쿼리 확장 (stats / minimap / bottom-tabs / fit-to-screen), guild-deaths / .guild-res-warn / stats-toggle 스타일

### 문서

- `docs/PM/DECISIONS.md` — D-2026-05-14-13 (D-12 closure), D-2026-05-14-14 (modifier 12)
- `docs/PM/STATUS.md` — 최근 활동 갱신, M4 마감 → M5 진입 명시
- `docs/PM/BACKLOG.md` — Done 행 추가 (D-12 closure / B-204 / B-201 / B-202 / B-203 / B-206 / M4 modifier 12), B-207 신규, B-205 closure 메모
- `docs/PM/handoffs/2026-05-14-m4-to-m5.md` — 이 문서 (NEW)

### 메모리

- `~/.claude/projects/-Users-bm-RLD/memory/feedback_design_tooling_preference.md` — 디자인 도구 선호 (외부 Figma 비추, Claude Artifact / Code 우선)

### 메타

- 메인 push 누적 8 커밋 (`68d715f..a1c62cd`) — origin/master 최신

---

## 7. 프로세스 메모

### M3 → M4 → M5 진행 시각화

```
M1 ✅ 그림 그리기 (3축 비판 + VISION)
M2 ✅ 정체성 카피 (B-001~004)
M3 ✅ T2B 본진 (시드 + modifier MVP 3 + 알고리즘=캐릭터)
        ↓ B-109 (modifier sim 통합)
M4 ✅ 모바일 (B-204/201/202) + 사망 페널티 (B-203) + modifier 12종
        ↓ B-207 (silent_q placeholder)
M5 ⏳ BGM 4트랙 + 한영 i18n + 출시 페이지 (~6~7개월 → 1.0)
```

### M5 → 1.0 게이트 (VISION §8)

| 게이트 | 기준 |
|---|---|
| M5 → 1.0 | 한영 i18n 누락 텍스트 **0건**. BGM 4트랙 페이드 정상. 출시 페이지 (itch.io + 웹) 작동. **자체 플레이테스트 첫 5분 합격률 80%+** (외부 플레이어 5명 기준). |

이 게이트가 1.0 출시 직전 마지막 관문.

### 메모리 정합성

- `[[project_kiosk_archived]]` — 키오스크 cycle 1 동결 유지 (2026-05-12 본선 탈락). RLD 본 레포 알고리즘 작업은 별개.
- `[[feedback_no_meta_lessons]]` — 시연 메타 멘트 금지. M5 영어 카피 작성 시 주의.
- `[[feedback_design_tooling_preference]]` — 출시 페이지는 Claude Code 직접 HTML 작성 권장 (Figma 비추).

---

**끝**. 이 문서 + 직전 PM 인계 4개 + STATUS + BACKLOG + VISION 7개로 zero-base 진입 가능. M4 완료, M5 1.0 폴리시 진입 준비 완료.
