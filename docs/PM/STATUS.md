# RLD — Project Status

**Last updated**: 2026-05-20 (M5 — W8~W15 i18n + toggle stale fix 누적, editor 영역 100% 완전 closure (composer onLangChange 재렌더 포함). in-game narrative 톤 결정 영역만 잔존)

## Current track

**완성된 게임으로 출시 노선**.
- 이전 노선: AI 해커톤 (aihackcamp-2026) — 폐기 (탈락).
- 현 노선: 일반 플레이어 대상 독립 게임. **출시 1.0 그림은 [VISION.md](./VISION.md)** 참조.
- 한 줄 정체성 (D-4 확정): **알고리즘이 곧 캐릭터인 시드 기반 로그라이크 — 매 런 다른 모디파이어 아래 한정된 세르파 풀로 던전을 답파한다. RL 교육은 부산물.**

## 활성 결정

없음. 다음 결정은 M4 작업 진행 중 자연 발생 예정.

## In-Progress

- **M1 ✅ 완료** — 그림 그리기 (3축 비판 + 디베이트 + VISION.md).
- **M2 ✅ 완료** — P0 4/4 (B-001~B-004). 정체성 정리 + Step-0 진입 모드 박힘.
- **M3 ✅ 완료** — P1 단기 8건 + T2B-3 + **T2B-1 ✅** (시드 일일 챌린지) + **T2B-2 MVP ✅** (모디파이어 3종 데일리 전용) + **B-109 ✅** (sim 측 ModifierSet 통합 + modifier-on 측정 통과).
- **M4 ✅ 완료** — D-12 closure + 모바일 트랙 + B-203 사망 페널티 + 모디파이어 12종. **B-205 자연 closure** (modifier 데일리 전용 유지로 캠페인 발란스 무위협).
- **M5 진입** — **B-207 silent_q placeholder ✅** + **한영 i18n 2차 ✅** + **B-208 출시 페이지 1차 ✅** + **B-208.1 본격 스크린샷 6장 ✅** + **B-208.2 itch.io 카피 초안 ✅** (사용자 검수 대기) + **B-208.3 GitHub Pages 활성화 ✅** + **W1 i18n sync-check ✅** + **W3 daily.compare 부호 표기 ✅** + **W4 tutorial.STEPS sync-check ✅** + **W5 dict-ko 메뉴 27건 한국어화 ✅** + **W6 식량 가시성 + sim baseline 가드 ✅** + 모바일 title overflow fix. 다음 항목: W2 토글 갱신 사각지대 스모크 (사용자 협력) / i18n 미터치 잔여 (dungeon-select option 27 / DUNGEON_HINTS ~50 — narrative 톤 결정 영역, 사용자 협업 필요. briefing W8 + editor HTML W9 + editor JS W10 마감) / BGM 4트랙 (사용자 결정 후) / 자체 플레이테스트 5회 (1.0 게이트) / W7 (오프닝 카드 + 첫 클리어 overlay + 스토리 docs — 사용자 협업 필요).

## 최근 활동

- 2026-05-20 — **W15 composer onLangChange 재렌더 fix (W14 innerHTML/title toggle stale 마지막 closure)** — W14 commit 의 *composer floor list 동적 render → toggle stale* 인계 사실을 코드 측 fix 로 마감. **main.js**: 기존 onLangChange 콜백 (line 195-202) 에 composer 재렌더 분기 추가 — `if (currentMode === 'editor' && editorSubtab === 'dungeon') { renderComposerFloors() }`. 가드 두 축으로 *editor 모드 + dungeon subtab visible* 일 때만 재렌더 — invisible composer 의 불필요 재렌더 회피. **결과**: composer empty 메시지 (`no_floors_yet`) + Remove floor/variant tooltip 둘 다 토글 자동 갱신. **editor 영역 100% 완전 closure** — W8/W9/W10/W11/W12/W13/W14 누적 마감 + W15 composer 토글 fix 로 *editor 영역 안의 모든 동적/정적 라벨* 이 언어 토글 시 자동 갱신. **검증**: node --check OK, sync-check exit 0 (parity 343 유지 — dict 변경 0). sim / 캠페인 발란스 / 결정론 무영향 (UI 토글 동작 fix 전용). **잔여 toggle stale 영역**: tutorial overlay / hire confirm / toast / in-game showMessage 류 (사용자 협업 영역, narrative 톤 결정 필요).
- 2026-05-20 — **W14 editor composer 잔여 영문 라벨 3개 i18n (자율 grep 전수 점검 후 추가 발견)** — W8~W13 사이클 마감 후 추가 grep 으로 발견된 *editor composer 영역 잔여 영문 3건* 마감. **dict 양사전 신규 3키 (parity 340 → 343)**: `editor.msg.no_floors_yet` (composer empty 상태 — "층이 없습니다. \"+ 층 추가\" 를 눌러 시작하세요." / "No floors yet. Click \"+ Add Floor\" to start.") + `editor.btn.remove_floor` (층 삭제 tooltip) + `editor.btn.remove_variant` (변형 삭제 tooltip). **main.js**: line 1343 `innerHTML No floors yet` → `t('editor.msg.no_floors_yet')` / line 1363 `removeBtn.title = 'Remove floor'` → `t(...)` / line 1405 `rmBtn.title = 'Remove variant'` → `t(...)`. **dict 동기 의무 명시**: `no_floors_yet` 카피 안에 `"+ 층 추가"` / `"+ Add Floor"` 문자열이 박혀 있어 `editor.btn.add_floor` 카피 변경 시 함께 갱신 필요 — ko/en 양쪽 dict 에 인라인 주석으로 명시. **innerHTML 토글 stale 잔존**: composer floor list 가 동적 render 라 toggle 시 *재 render 안 됨* (사용자가 + Add Floor / Remove 클릭 시 갱신). W2 사각지대 인계. **검증**: sync-check exit 0 (modifiers 12 / characters 15 / items 3 / chapters 7 / tutorial 5 / ko 343 / en 343), node --check OK, `innerHTML.*No floors` / `title = 'Remove` 잔여 grep clean (**editor 영역 범위 한정** — 전 코드베이스 0 의미 X). sim / 캠페인 발란스 / 결정론 무영향 (UI dict + t() 호출 전용). **자율 진행 상태**: editor 영역 100% i18n 마감 + **editor 영역 잔여 영문 grep clean**. 자율 가능 영역 소진 — 잔여는 **narrative 톤 결정 영역**: in-game showMessage ~28건 (main.js line 1831 / 2279~2308 / 2425~2461 / 2578~2596 / 2747~2820 / 2854 / 3125 / 3144 / 3186 / 3414 / 3913 / 3966 등) / dungeon-select option 27 / DUNGEON_HINTS ~50 / farming 영역 / tutorial / hire confirm / toast 류 — D-2026-05-19-1 §RL 메타 흡수 룰 + STORY.md 톤 정합 필요, 사용자 협업 사이클 영역.
- 2026-05-20 — **W13 _lastEditorRender lifetime reset + dict 주석 stale 정정 (W12 리뷰어 W-1/I-1 closure, 작은 정합 사이클)** — W12 리뷰어 권장 조치 둘을 한 사이클로 흡수. **main.js switchMode**: editor 모드를 떠나는 두 경로 (daily / play 진입 시 prevMode === 'editor') 에 `this._lastEditorRender = null` 박음. briefing.js 의 `hide() { _lastShowArgs = null }` 대칭 패턴 — *editor 영역 떠난 후 토글* 시 보이지 않는 div 갱신 (무해하지만 불필요) 회피. **dict-ko/en 주석 stale 정정**: line 428/417 의 `errors 배열 push, main.js 의 errors.join 으로 사용자 노출` 표현을 W12 후 실제 패턴인 `{ key, params? } 객체 push, main.js 의 showEditorErrorsMessage 에서 errors.map(e => t(e.key, e.params)).join 으로 매 토글마다 재평가` 로 갱신. **검증**: sync-check exit 0 (parity 340 유지 — 주석만 변경, 키 변경 0), node --check OK, _lastEditorRender reset 호출 3건 (init 1 + daily/play 진입 2). sim / 캠페인 발란스 / 결정론 무영향 (UI lifetime 정합 + 주석 갱신 전용). **잔여 W2 사각지대**: tutorial overlay / hire confirm / toast 류 동적 메시지 (사용자 협업 영역, 자율 진행 X).
- 2026-05-20 — **W12 editor.js errors 배열 키 박힘 재설계 + main.js thunk 패턴 통합 (W11 비대칭 결손 closure)** — W11 리뷰어 W-1 (errors.join 비대칭 stale) 별 사이클 분리 권장 흡수. **editor.js**: errors 배열 6 push 를 `{ key, params? }` 객체로 변경 (validate 4 + startQuickTest 2). 호출 시점 t() 결과를 박지 않고 *키 박음* — main.js 의 join 시점에 매번 t() 재평가, 토글 시점에 *현 시점 언어로 자동* 평가. t import 는 Custom Dungeon default name 1건 호출 (line 843) 위해 유지 — 그 호출은 onPlayDungeon 시점 1회라 토글 stale 무관. **main.js**: W11 의 `_lastEditorMessage` (단일 키 캐시) 를 더 일반적인 `_lastEditorRender` (thunk 캐시) 패턴으로 통합. 신규 메서드: `_renderEditorMessage(thunk, type)` (thunk 캐시 + 즉시 실행) / `showEditorI18nMessage(key, type, params)` (thunk 래핑) / `showEditorErrorsMessage(errors, type, prefixKey?)` (errors map+join, prefix 키 보간 옵션). 호출처 4건 변경: `result.errors.join(', ')` 3건 → `showEditorErrorsMessage(result.errors, 'danger')`, `fix_errors_first {errors}` 1건 → `showEditorErrorsMessage(result.errors, 'danger', 'editor.msg.fix_errors_first')`. onLangChange 콜백도 thunk 재호출로 단순화. **결과**: editor 영역 *모든 22 동적 메시지* (W11 의 20 + W12 의 4건 errors / 일부 중복) 가 *토글 시 자동 갱신* — W11 의 비대칭 결손 closure. **검증**: sync-check exit 0 (parity 340 유지 — dict 변경 0), node --check OK, errors.join 패턴 grep 0 (전부 errors.map(e => t(e.key, e.params)).join 형태로 일관). sim / 캠페인 발란스 / 결정론 무영향. **잔여 toggle stale**: tutorial overlay / hire confirm / toast 류 동적 메시지 — W2 5 시나리오 스모크 시점에 일괄 점검 (사용자 협업 영역).
- 2026-05-20 — **W11 onLangChange 구독으로 briefing + editor message toggle stale 코드 fix** — W8/W10 사이클 인계된 *동적 메시지 stale invariant* 의 코드 측 fix. **briefing.js**: `onLangChange` import + constructor 에 구독 추가, `_lastShowArgs` 캐시 ([dungeonId, runState, charName, getDungeonDisplayName]). 언어 토글 시 `isVisible && _lastShowArgs` 면 마지막 인자로 `show()` 재호출 → 모든 13 t() 호출이 다시 평가되어 라벨 자동 갱신. `hide()` 에서 `_lastShowArgs = null` 로 hidden 상태 stale 회피. **main.js**: `onLangChange` import + 신규 메서드 `showEditorI18nMessage(key, type, params)` 신설 — t() 결과를 textContent 박는 `showEditorMessage` 위에 i18n key 캐시 레이어. `_lastEditorMessage = { key, type, params }` 저장. constructor 의 BriefingOverlay 생성 직후에 `onLangChange` 구독 — toggle 시 `_lastEditorMessage` 가 있으면 `t(key, params)` 재계산해서 `showEditorMessage` 호출. **호출처 20건 치환**: `this.showEditorMessage(t('editor.msg.X', params), type)` → `this.showEditorI18nMessage('editor.msg.X', type, params)`. `result.errors.join(', ')` 3건 (line 1046 / 1098 / 1119) 은 *비-i18n 메시지* 라 그대로 `showEditorMessage` 유지 — errors 배열은 editor.js 시점 t() 결과라 toggle 시 stale 잔존 (별 사이클로 인계). **시각 스모크는 W2 사이클 유지** — 본 사이클은 *코드 측 fix 만* 자율 마감, 5 시나리오 실 브라우저 검증은 사용자 협업 영역. **검증**: sync-check exit 0 (parity 340 그대로 — dict 변경 0), node --check main.js / briefing.js OK, `showEditorMessage(t(...))` 호출처 grep 결과 0 (메서드 내부 sink 1건만). sim / 캠페인 발란스 / 결정론 무영향 (UI 토글 동작 fix 전용). **잔여 toggle stale 영역**: editor.js validation errors (errors.join 의 키 미박힘) / tutorial overlay / hire confirm / toast 류 동적 메시지 — W2 사이클 시점에 일괄 점검.
- 2026-05-20 — **W10 editor JS 동적 메시지 i18n 마감 (D-2026-05-15-16 미터치 영역 2.5차 closure)** — `main.js` 의 `showEditorMessage(...)` 호출 22건 + `editor.js` 의 validation errors 6건 + Custom Dungeon default 1건 = **25 메시지 t() 치환**. **dict 양사전 신규 25키 (parity 315 → 340)**: `editor.default.custom_dungeon` (1) + `editor.msg.*` (18: grid_resized {w,h} / grid_cleared / valid_ready / enter_stage_name / enter_dungeon_name / fix_errors_first {errors} / saved {name} / loaded {name} / deleted / add_floor / floor_no_stage {floor} / floor_variant_empty {floor,variant} / saved_dungeon {name} / dungeon_not_found / loaded_dungeon {name} / dungeon_deleted / resolve_dungeon_failed / max_floors) + `editor.err.*` (6: no_grid / start_missing / goal_missing / no_path / test_running / qt_not_configured). **카피 분리**: `Enter a dungeon name` 영문 메시지가 stage save (`btn-save-dungeon`) + dungeon comp save (`btn-save-dungeon-comp`) 두 영역 동일하게 사용되던 것을 한국어 카피로 자연스럽게 분리 — `enter_stage_name` ("스테이지 이름을 입력하세요") / `enter_dungeon_name` ("던전 이름을 입력하세요"). en 측은 stage 영역 카피만 `Enter a stage name` 으로 미세 수정. **`editor.js` 변경**: i18n import 추가 (line 7), validate() 의 errors 배열 push 4건 → `t('editor.err.*')`, startQuickTest 의 errors 2건 → `t('editor.err.*')`, playDungeon 의 `'Custom Dungeon'` default → `t('editor.default.custom_dungeon')`. **`main.js` 변경**: line 1031~1304 의 22 호출처를 `t('editor.msg.*', { ...params })` 로 치환 (보간값: w / h / errors / name / floor / variant). `showEditorMessage` 내부는 `el.textContent = text` 이라 XSS 안전, t() 충분 (tHtml() 불필요). **검증**: sync-check exit 0 (modifiers 12 / characters 15 / items 3 / chapters 7 / tutorial 5 / ko 340 / en 340 parity ✓), `node --check main.js / editor.js` OK, 잔여 영문 `showEditorMessage('[A-Z]` grep clean, editor.js의 영문 errors push grep clean. sim / 캠페인 발란스 / 결정론 무영향 (UI 동적 메시지 dict + i18n 호출 전용). **W2 토글 갱신 사각지대 인계**: editor 메시지는 *발생 시점의 t() 결과 1회* 사용 — 메시지 노출 중 언어 토글 시 stale (textContent 한 번만 박힘). W2 5 시나리오에 "editor 작업 → validate 클릭 → 메시지 노출 중 언어 토글" 케이스 추가 권장. **D-2026-05-15-16 closure 진행률**: 4 영역 중 2.5 (briefing 100% + editor HTML+JS 100% + Custom Dungeon default 100%). 잔여: dungeon-select option 27 / DUNGEON_HINTS ~50 (둘 다 narrative 톤 결정 영역, 사용자 협업 필요).
- 2026-05-20 — **W9 editor 패널 HTML 정적 라벨 i18n 마감 (D-2026-05-15-16 미터치 영역 2차 closure)** — Stage 패널 + Dungeon Composer 패널의 영문 박힌 정적 라벨 40개에 `data-i18n` / `data-i18n-placeholder` 박기. **dict 양사전 신규 40키 (parity 275 → 315)**: `editor.tab.{stage,dungeon}` (2) + `editor.head.*` (10: grid_size / tile_palette / tools / actions / stage_library / quick_test / shortcuts / dungeon_composer / floors / rules) + `editor.btn.*` (14: apply / undo / redo / clear / validate / save / load / delete / play_stage / save_dungeon / play_dungeon / add_floor / quick_test / stop) + `editor.tool.*` (3: brush / eraser / fill) + `editor.placeholder.*` (2: stage_name / dungeon_name) + `editor.option.*` (2: select / select_dungeon) + `editor.qt.*` (3: character / episodes / show_policy) + `editor.short.*` (2: select_tile / erase — Undo/Redo 는 btn.* 재사용) + `editor.rule.*` (2: hp_carry / gold_on_clear). **D-4 학명 보존 영역**: `editor.qt.episodes` ("에피소드:" — W7.3 narrative 흡수 룰은 in-game 카피 한정, editor 는 D-3 던전 마스터 모드 = 개발자 영역이라 학명 유지) / `editor.rule.hp_carry` ("HP 이월" — HP 게임 관례 약어). **D-15 영문 톤**: Adventurer-flavored 어휘 0건, 짧은 명사구. **W:/H:** Grid Size 의 1자 약어는 게임 관례 보존 (briefing 의 stat.hp 와 동일 룰). **별 사이클 분리**: editor.js + main.js 의 `showEditorMessage(...)` 동적 메시지 ~25건 (Saved/Loaded/Deleted/Valid!/Fix errors first/Floor N has no stage 등) 은 별 사이클 (briefing 의 tHtml 패턴 응용 — 동적 보간 + innerHTML escape 가드 필요). 던전 선택 option 27 (Lv.X English names) + DUNGEON_HINTS ~50 도 별 사이클 (narrative 톤 결정 영역). **검증**: sync-check exit 0 (modifiers 12 / characters 15 / items 3 / chapters 7 / tutorial 5 / ko 315 / en 315 parity ✓), editor 영역 잔여 영문 grep clean (Character options 의 한국어 이름 + 영문 알고리즘명은 `character.*` 키 영역 별도, 본 사이클 범위 밖). sim / 캠페인 발란스 / 결정론 무영향 (UI dict + HTML data-i18n 박기 전용).
- 2026-05-20 — **W8 briefing overlay 영문 라벨 i18n 마감 + tHtml helper 도입 (D-2026-05-15-16 미터치 영역 1차 closure + W1 Warning fix)** — 던전 진입 전 BriefingOverlay (Darkest Dungeon 스타일) 의 영문 박힌 라벨 13개를 t() 호출로 치환. **dict 양사전 신규 13키 (parity 262 → 275)**: `overlay.briefing.entry_cost` (입장료 / Entry Cost) + `first_reward` (첫 답파 보상 / First Clear Reward) + `repeat_reward` (반복 보상 / Repeat Reward) + `train_cost` (학습 비용 / Train Cost — D-4 학명 "학습" 보존) + `treasure` (보물 / Treasure) + `modifiers` (모디파이어 / Modifiers) + `your_gold` (보유 골드 / Your Gold) + `food` (식량 / Food) + `mod_slippery` ([미끄러움] / [Slippery]) + `mod_hp_aware` ([HP 인지] / [HP-Aware] — HP 학명 약어 보존) + `hints_title` (힌트 / Hints) + `provisions_title` (신속 보급 / Quick Provisions) + `food_buy` (`+{amount} 식량 ({cost}G)` / `+{amount} Food ({cost}G)` — tHtml() 보간). **briefing.js**: `rewardLabel` 분기 + `slippery`/`hpAware` 모디파이어 태그 + 6 detail row 라벨 + Hints 섹션 타이틀 + Quick Provisions 섹션 타이틀 + 2 food-buy 버튼 모두 t()/tHtml() 로 치환. 인라인 영문 박힘 0건 (코드 주석 `// Hints` 1건만 잔존 — 코드 주석은 영문 OK). **tHtml helper 신설 (i18n/index.js)** — innerHTML 경로 전용 보간 sink. dict 값 자체는 trusted (개발자 작성) 라 escape 안 함, params 값만 HTML escape (`&` `<` `>` `"` `'`) — 향후 외부 입력 (캐릭터명 등) 보간 시 XSS 가드. food_buy 호출 2건만 tHtml() 적용 (현재 params 보간 유일 호출처), 나머지 11건은 t() 유지 (params 없음). 리뷰어 (W1 Warning) 권장 조치 본 사이클 흡수. **검증**: sync-check exit 0 (modifiers 12 / characters 15 / items 3 / chapters 7 / tutorial 5 / ko 275 / en 275 parity ✓), `node --check briefing.js / index.js` OK, 잔여 영문 라벨 grep clean. sim / 캠페인 발란스 / 결정론 무영향 (UI dict + briefing.js 라벨 치환 + i18n helper 신설 전용). **W7 narrative 카피 흡수 룰 정합** — train_cost 의 "학습" / mod_hp_aware 의 "HP" 는 D-4 학명 보존 룰 (한국어 "탐험" 어휘는 narrative 흡수 영역, briefing 의 "Train Cost" 는 길드장 시스템 라벨이라 학명 유지). **D-2026-05-15-16 미터치 4 영역 중 1 영역 closure** — 잔여: editor 패널 (~30) / dungeon-select option (27) / DUNGEON_HINTS (~50).
- 2026-05-19 — **B-208.1 산출물 6장 재캡처 (W7.3 카피 흡수 반영)** — `tools/screenshots/capture-play.mjs` 재실행 (PRESEED `rld_opening_seen: 'true'` 박혀서 W7.1 오프닝 카드 자동 dismiss). 모든 6장 갱신: ① play-training — subtitle "세르파 길드 — 길드장의 기록" + sparkline label "탐험 성공률" 반영 (이전 "강화학습 던전 탐험 / 학습 성공률"), ② play-daily — subtitle 반영, ③ play-party — 변경 없음 (캐릭터 그리드), ④ play-quest — 변경 없음 (퀘스트 탭), ⑤ play-gameover — subtitle + game-over note "세르파의 기억은 보존됩니다. 다음 시도에서 더 멀리 갈 겁니다." + 탐험 성공률 label 반영 (이전 "Q-테이블은 보존됩니다 / 학습 성공률"), ⑥ play-daily-mobile — subtitle 반영. **narrative 일관성 회복** — D-2026-05-19-1 §10 RL 메타 흡수 룰 + STORY §RL 메타 흡수 룰 의 시각 증거 갱신. landing 페이지의 4 grid img (commit 6d5bc04) 도 자동 동기 (같은 PNG 파일 참조). sim / 캠페인 발란스 무영향 (산출물 전용).
- 2026-05-19 — **W7.1 오프닝 카드 신설 (게임 시작 narrative)** — 첫 새 게임 1회 표시되는 full-screen overlay. D-2026-05-19-1 §1~7 + STORY.md §사건 흐름 직접 implementation. **구성**: 헤딩 "세르파 길드" + 6 단락 narrative (① 모험가 길드 그늘 / ② 아빠 = 영웅 + 세르파 길드 창립자 + 던전 깊은 곳의 미지 + 귀환 아이템으로 아들 탈출 후 본인 실종 / ③ 시간 흐름 + 길드 명맥 / ④ 모험가 길드의 부추김 — 아빠가 뭔가 남겼다는 추측 / ⑤ 부활 아이템 = 유산 / ⑥ "다시 시작할 시간이다") + "시작" 버튼. **신규 자산**: `web/play.html` `#opening-overlay` (body 직속, z 1000) + `web/css/style.css` `.opening-overlay`/`.opening-box`/`.opening-heading`/`.opening-start` 스타일 (backdrop rgba(8,8,14,0.98) — bleed-through 차단, max-width 640px 데스크탑 + 모바일 700px 미디어 쿼리). **JS 진입 흐름**: `web/js/main.js` btn-new-game 핸들러 안 `_showOpeningCard(onClose)` helper + `rld_opening_seen` localStorage flag 1회 박제. `tutorial.tryShow('init')` 호출을 constructor (기존 line 237) → enterGuild 콜백 안으로 이동 — opening card 와 시점 충돌 해소 (btn-continue 핸들러도 동일). **dict 양사전 8 키 신규** (parity 254 → 262): `opening.heading` + `opening.p{1..6}` + `opening.start`. en 카피는 D-2026-05-15-15 Crisp 톤, "Adventurers' Guild" 는 적의 이름 (D-15 의 "Adventurer-flavored 회피" 룰 = 게임 톤 측면이라 무위반). **자동화 갱신**: `tools/screenshots/capture-play.mjs` PRESEED 에 `rld_opening_seen: 'true'` 추가 — 향후 자동 캡처 시 카드 자동 dismiss. **검증**: sync-check exit 0 (ko 262 / en 262 parity ✓), node --check main.js OK, Chrome headless 1280×900 데스크탑 + 420×900 모바일 둘 다 카드 시각 정상 + 환영 토스트 시점 충돌 해소 (이전 시도에서 토스트가 backdrop bleed-through 로 보이던 결함 fix). sim / 캠페인 발란스 무영향 (UI + dict 카피 전용). W7.4 모험가 도움 시스템 진입 전 narrative 진입점 마감.
- 2026-05-19 — **W7.3 RL 메타 카피 흡수 (in-game) + landing alt 톤 정렬** — D-2026-05-19-1 §10 + STORY.md §RL 메타 흡수 룰 직접 implementation. **dict 양사전 9 키 변경**: ① `document.title` ("RL Dungeon - 강화학습 던전" → "RL Dungeon — 세르파 길드"), ② `dev.subtitle` ("강화학습 던전 탐험 — Dev Mode" → "세르파 길드 — 길드장의 기록"), ③ `overlay.game_over.note` ("Q-테이블은 보존됩니다. 세르파는 모든 것을 기억합니다." → "세르파의 기억은 보존됩니다. 다음 시도에서 더 멀리 갈 겁니다."), ④ `overlay.ending.note` (Q-테이블 → 세르파의 기억), ⑤ `sparkline.label` ("학습 성공률" → "탐험 성공률"), ⑥ `tutorial.ai_training` ("AI 학습 ... 에피소드마다" → "세르파는 ... 매 탐험마다"), ⑦⑧ `item.{defense_contract,trap_nullify}.desc` ("1에피소드" → "1탐험"), ⑨ `landing.screenshot.training.alt` (Warning 1 어순 fix). **dict-en.js line 1~15 톤 가이드 주석 갱신** — 기존 "게임오버 메모 'Q-table is preserved' 같은 학명 그대로" → "In-game 카피는 narrative 톤 (학명 회피 / hover B-105 opt-in / landing.meta + document.title 외부 SEO 별 룰)". **외부 SEO 영역 별 룰**: `landing.meta.*` (D-4 한 줄 정체성 직인용 마케팅) + `document.title` 의 "RL Dungeon" 브랜드 자체는 유지, narrative 시그니처 ("세르파 길드") 만 추가. **검증**: sync-check exit 0 (ko 254 / en 254 parity ✓), 잔존 학명 = 톤 가이드 주석 본문 1건 (의도된 언급). landing 커밋 (6d5bc04) reviewer Warning 1+2 closure. sim / 캠페인 발란스 / 결정론 무영향 (UI 카피 전용). **visual 회귀는 별 사이클** — W7.1 오프닝 카드 박힌 후 통합 캡처 (B-208.1 산출물 6장 갱신 묶음).
- 2026-05-19 — **W7.6 STORY.md 박제** — `docs/PM/STORY.md` 신규 (한 페이지). D-2026-05-19-1 본문을 풀어서 narrative 통합 문서. 구성: §한 줄 요약 + §등장인물/진영 5 (길드장 / 아빠 / 모험가 길드 / 세르파 길드 / 세르파) + §사건 흐름 (과거 → 현재 → 게임 시작 → 진행 → 엔딩 분기 굿/베드) + §게임 시스템 ↔ narrative 매핑 표 (12 시스템) + §답파 동기 + **§RL 메타 흡수 룰 5개** (① 표면 카피 학명 회피 / ② 학명은 hover B-105 opt-in / ③ Q-heatmap/sparkline = 길드장 옵션 / ④ 시스템 카피 narrative 어휘 — "에피소드"→"탐험", "AI 학습"→"세르파 훈련" / ⑤ D-4 "RL 교육은 부산물" 보존) + §카피 톤 가이드 (한국어=차분+그늘, 영문=D-15 Crisp + Adventurer-flavored 회피) + §미정 7건 (모험가 도움 효과 강도 / manual vs AI UI 구분 / 진엔딩 조건 / 흑막 폭로 시점 / NG+ narrative 위치 / 부활 아이템 출처 / 게임 시작 계기). **모든 후속 게임 카피의 유일한 출처** — 변경 시 본 문서 + DECISIONS.md 동시 갱신 룰 박힘. sim / 캠페인 발란스 무영향 (PM 문서 전용).
- 2026-05-19 — **D-2026-05-19-1 narrative 박제 + W7 분할** — W6 commit message 끝 박힌 세계관 재해석을 W7 사이클에서 정합 점검 + 디테일 채워 D-* 로 박음. **핵심**: ① 세르파 = 길드장의 *부활 아이템* 하에 던전에 가는 *비-영웅 인간* (모험가 못난이 출신, 본인은 부활 능력 X), ② 두 길드 = 모험가 길드 (기득권/흑막) vs 세르파 길드 (반발로 창립, 아빠 창립자), ③ 아빠 = 던전의 *모더* (모디파이어 12종 = 아빠 시련으로 narrative 흡수), ④ 누적 사망 4/4 = 부활 아이템 충전 한도로 narrative 흡수, ⑤ 모험가 도움 (신규) = shop 일회용 보너스, **manual play 시 모험가 동반 = 즉시 베드 엔딩** (모험가가 길드장 위치 노출 → 살해 → 부활 아이템 약탈), AI 학습 동반은 안전 (길드장 부재), ⑥ RL 메타 흡수 룰 = 표면 카피에서 학명 회피, hover 툴팁 (B-105) 만 opt-in, Q-heatmap/sparkline = 길드장 옵션 (D-4 부산물 보존). **귀환 아이템 ≠ 부활 아이템** (별개 — 귀환은 아빠가 아들 탈출 시 소비, 부활은 아빠 유산). **D-4 정체성 모든 축과 정합** ✓ ([[feedback_no_meta_lessons]] 도 준수). **W7 분할**: W7.0 ✅ / W7.1 오프닝 카드 / W7.2 첫 클리어 overlay / W7.3 RL 메타 카피 흡수 (~5건) / W7.4 모험가 도움 시스템 (가장 큰 사이클, 사용자 협업) / W7.5 진엔딩 / W7.6 STORY.md. **미정 잔여 (W7.4 시작 시 결정)**: 모험가 도움 효과 강도 / manual vs AI 학습 명시적 UI 구분 / 진엔딩 조건 / 흑막 폭로 시점 / NG+ narrative 위치. 카피/코드 변경 0 (PM 문서 박제 전용).
- 2026-05-19 — **landing screenshot grid 갱신 (B-208 1차 후속)** — B-208 1차의 2×2 placeholder (점선 박스 4) 를 B-208.1 본격 산출물 4장으로 교체. `web/index.html`: `<div class="screenshot-placeholder">` ×4 → `<img class="screenshot-figure" data-i18n-alt="...">` ×4 (play-training / play-daily / play-party / play-quest; gameover/daily-mobile 은 별 사이클). `web/js/i18n/index.js applyTranslations`: `data-i18n-alt` 패턴 신규 (기존 `data-i18n` / `-title` / `-placeholder` / `-aria-label` 과 동일 패턴, setLang 시 자동 적용). dict 양사전: `landing.screenshot.{training,daily,party,quest}.alt` 4키 추가 + `landing.screenshots.coming` 1키 제거, parity 251 → 254 ✓. `web/css/landing.css`: `.screenshot-placeholder` (점선 박스) → `.screenshot-figure` (img 친화, aspect-ratio 16:10 데스크탑 / 16:9 모바일, object-fit cover + center top). **검증**: sync-check exit 0, Chrome headless 1400×900 데스크탑 + 420×900 모바일 4장 노출 정상, placeholder DOM 잔존 0건. sim / 캠페인 발란스 무영향 (UI only). **리뷰 (korean-git-reviewer)**: Critical 0, Warning 3 (training alt 어순 / party alt hire 중복 → W7.3 사이클에서 일괄 정리, HTML alt 한글 하드코딩 = ko fallback 정합). 커밋 `6d5bc04`.
- 2026-05-19 — **B-208.1 본격 스크린샷 6장 자동화** — Chrome DevTools Protocol (CDP) WebSocket 직접 호출로 `web/play.html` 의 6 시나리오 캡처. **시나리오**: ① 학습 시각화 (Q-heatmap 컬러 + Q-values 6셀 가시 — 학습 stochasticity 로 매 실행 변동 + sparkline 100% + "Converged! Clear: 100% after 20 episodes"), ② 모디파이어 데일리 (이번 런: 치명상 ×1.5 모디파이어 띠 + 20×20 fog-of-war 던전), ③ 캐릭터 다양성 (Hired Q군 + Available for Hire 사르사/몬테/트레이서/다이나/그래디/크리틱 — 한국어 이름 + 영문 알고리즘명 + Op 비용), ④ 던전 다양성 (Ch.1 첫 발걸음 / Ch.2 위험한 길 — Lv.1 Tutorial NEW + Lv.2~6 LOCKED), ⑤ 게임오버 오버레이 (GAME OVER + HP 0 + 누적 사망 1/4 + Q-테이블 보존 + 새 런), ⑥ 모바일 데일리 (420×900 fit + 모디파이어 띠). **자동화**: `/tmp/rld-shots.mjs` — CDP WebSocket, dependency 0, node 22 내장 WebSocket. 진입은 `window.game.{screenManager.show / loadDungeon / startTraining / enterDailyMode / switchMode / triggerGameOver}` 직접 호출. **핵심 학습**: ① 캔버스/오버레이는 `screen-dev` 안 (screen-guild 는 quest/party/shop/map 탭만), ② `trainingSpeed = 0` (instant) 가 동기 루프로 5×5 던전 20 episodes 만에 수렴, ③ 학습 종료 후 `renderer.fogOfWar = false; showQValues = true; render()` 명시 호출로 Q-heatmap 컬러 캡처, ④ localStorage `rld_tutorial` preseed 로 첫 진입 토스트 차단. **산출물**: `web/assets/screenshots/play-{training,daily,party,quest,gameover,daily-mobile}.png` (6장, 평균 150KB). 기존 dev artifact 4장 (landing-* / play-title*) 별도. landing.html 의 screenshot grid placeholder (B-208 1차) 를 본 산출물로 교체 가능 — 별 사이클. sim / 캠페인 발란스 무영향 (산출물 전용).
- 2026-05-17 — **W6 식량 가시성 (F1+F2) + sim baseline 가드** — 사용자 보고 ("식량 가시성 X / 떨어지면 즉사 / 가장 관리해야 할 자원") 직결. **결함 2개 발견**: ① B-204 모바일 룰이 `food-stat` data-priority `extra` 로 숨김 → `core` 격상, ② `STARTING_FOOD = 0` 초기값 결함 (첫 진입 즉사 위험) → `STARTING_FOOD = 100` 상수 신설. **sim 결합 침범 발견 (사용자 의심 정확)**: `sim/simulator.js` constructor 가 `new RunState()` 직접 호출 → STARTING_FOOD 변경이 sim baseline 침범. constructor 직후 `this.runState.food = 0` 명시 리셋해 HybridPlayer buyFood 흐름 (캠페인 25.5/27 ± 2, D-2026-05-14-14) 보존. **UX 보강**: `.food-value.food-low` (< 50 노랑) / `.food-critical` (< 20 빨강 pulse), `_flashFoodDelta(delta)` floating "-2" 1초 fade, `_foodWarnShown` first-time 토스트 (`food.warn.threshold`). **i18n 4키 신규** (양사전 동기 247→251): `food.warn.threshold` / `game_over.starvation` / `rope.escape` / `rope.escape_with_treasure`. 영문 하드코딩 메시지 3건 i18n 마감 (보너스). **검증**: sync-check exit 0 (modifiers 12 / characters 15 / items 3 / chapters 7 / tutorial 5 / ko 251 / en 251), `node sim/run-balance.js --runs=5` HybridPlayer Avg = 25.6/27 ✓ (sim baseline 보존 실측). W2 stale 주석 정정 (style.css mobile core stat 목록 갱신). 메모리 [[project_rld_runstate_sim_coupling]] 박힘. 커밋 `d520b8e`.
- 2026-05-15 — **B-208.3 GitHub Pages 활성화 + Actions deploy workflow** — 모바일 실기 테스트 / 외부 공유용 영구 공용 URL 확보. `.github/workflows/pages.yml` 신규 — `master` push 시 `web/` 디렉토리만 GitHub Pages 에 deploy (actions/configure-pages@v5 / upload-pages-artifact@v3 / deploy-pages@v4). paths 필터 (`web/**` + workflow 자체) 로 sim/docs 변경 시 deploy skip. concurrency `pages` 그룹 (동시 deploy 차단). 권한 최소 (contents:read, pages:write, id-token:write). **Pages site 활성화**: gh CLI 의 `kalliroku` 계정 (admin: true) 으로 전환 후 `gh api --method POST /repos/kalliroku/RLD/pages -f build_type='workflow'` — 응답 `html_url: https://kalliroku.github.io/RLD/`, `build_type: workflow`, `https_enforced: true` ✓. **URL**: 랜딩 https://kalliroku.github.io/RLD/ / 게임 https://kalliroku.github.io/RLD/play.html. README 의 "github pages — `kalliroku.github.io/RLD/` (예정)" → "(자동 배포)" 갱신. **모바일 실기 테스트 경로**: LAN IP (`http://172.30.1.66:8765/`, 같은 WiFi 한정) + GitHub Pages 공용 URL (외부 데이터망 가능). B-208 인계의 §3 B-208.3 closure.
- 2026-05-15 — **W5 dict-ko 영문 fallback 핵심 메뉴 한국어화 (27건)** — *한국어 모드에서 영문 노출되던 메뉴/탭/통계/오버레이* 한국어로 마감. **변경 키 (dict-ko 단방향, dict-en 미터치)**: title (3: 새 게임/이어하기/개발자 모드) + guild.tab (4: 퀘스트/파티/상점/지도) + dev.back_to_game + mode (2: 플레이/에디터) + overlay.game_over.note ("Q-테이블은 보존됩니다. 세르파는 모든 것을 기억합니다.") + overlay.game_over.new_run + overlay.ending.title (모든 던전 답파!) + overlay.ending.note ("Q-테이블은 보존됩니다. 자신의 기록을 갱신해보세요.", reviewer 톤 통일) + overlay.briefing (2: 뒤로/출진) + overlay.map_choice (3: 첫 클리어!/지도 판매/지도 보관) + stat (6: 런/골드/식량/스텝/보상/클리어율, HP 약어 보존) + food_warning + footer.controls_hint. **영문 보존 (게임 관례, 인라인 주석 명시)**: `GAME OVER` (오버레이 타이틀) / `New Game+` (NG+) / `lang.toggle` 의 `EN` (다음 언어 표시) / `HP` (게임 관례 약어) / 학명 `Q-테이블` (D-4 학명 보존). **검증**: `node web/js/i18n/sync-check.mjs` exit 0 (modifiers 12 / characters 15 / items 3 / chapters 7 / tutorial 5 / ko 247 / en 247, 누락 0). Chrome headless 1280×800 + 420×900 캡처 — "새 게임 / 이어하기 / 개발자 모드" 한국어 정상 노출. sim / 캠페인 발란스 무영향 (UI only). D-2026-05-15-16 미터치 영역 (briefing 영문 / editor / dungeon-options / DUNGEON_HINTS ~100키) 은 별 사이클 유지.
- 2026-05-15 — **모바일 title logo overflow fix** — `.title-logo` 가 `font-size: 3.5rem; letter-spacing: 6px` 고정 → 420px viewport 에서 "RL DUNGEON" 끝 글자 N 잘림 (Chrome headless 캡처로 발견). `@media (max-width: 700px)` 안에 `font-size: 2.2rem; letter-spacing: 3px` 추가 + 보수적 `text-align: center; max-width: 100%` 가드. 700px breakpoint 는 B-201/B-202/B-204 일관. **검증**: Chrome headless 1280×800 (데스크탑 미영향) + 420×900 (모바일 "RL DUNGEON" 깨끗하게 fit, New Game/Continue 버튼 정렬 정상). JS / sim / i18n / 캠페인 발란스 무영향 (CSS only). 커밋 `8ba0954`.
- 2026-05-15 — **W4 sync-check 범위 확장 (tutorial.STEPS 5종 closure)** — W1 사각지대 마감. `web/js/game/tutorial.js` 의 `const STEPS` → `export const STEPS` (sync-check 가 import 가능하게 노출). `web/js/i18n/sync-check.mjs`: `import { STEPS as TUTORIAL_STEPS } from '../game/tutorial.js'` + 5번째 카테고리 (각 STEPS 엔트리에 대해 `tutorial.{id}` 키 ko/en 양쪽 박힘 검증). 기존 #5 parity check → #6 으로 재번호. summary 에 `tutorial: ${TUTORIAL_STEPS.length}` 노출. **검증**: `node web/js/i18n/sync-check.mjs` exit 0 (modifiers 12 / characters 15 / items 3 / chapters 7 / **tutorial 5** / ko 247 / en 247 / 누락 0). `tutorial.dismiss` 고정 키는 parity check 가 자동 커버 (양 사전에 이미 박힘). sim / 캠페인 발란스 무영향 (build-time assertion 전용). W1 인계 §3 자율 빠른 마감 항목 1건 closure.
- 2026-05-15 — **W1 i18n sync-check + W3 daily.compare 부호 표기 마감** — i18n 1.0 게이트 후속 둘 마무리. **W1**: `web/js/i18n/sync-check.mjs` 신규 (95 lines). 객체 사전 (MODIFIERS 12 / CHARACTERS 15 / ITEMS 3 / CHAPTER_CONFIG 7) 의 ID 가 dict-ko + dict-en 양쪽에 모두 i18n 키로 박혔는지 build-time assertion. 4 카테고리 + 양사전 parity 검증. `node web/js/i18n/sync-check.mjs` 실행 결과 exit 0 (modifiers 12 / characters 15 / items 3 / chapters 7 / ko 247 / en 247 / 누락 0). 출시 직전 자동화 가능 (pre-commit hook / CI step). **W3**: `web/js/main.js handleDailyVictory` line 929 의 `daily.compare.better` 호출처에 `Math.abs(diff)` 전달. dict 양사전 카피 변경: `daily.compare.better` = `'어제보다 {diff} 스텝 빠름'` / `'{diff} steps under yesterday'`, `daily.compare.worse` = `'어제보다 {diff} 스텝 느림'` / `'{diff} steps over yesterday'`. 사유: 영문 `'{diff} steps vs yesterday'` 가 `diff=-3` 일 때 `"-3 steps vs yesterday"` — 의미상 정확하나 영어권 직관 어색. **검증**: node --check main.js / dict-ko / dict-en 모두 OK. sync-check 통과. sim 무영향 (UI 메시지 전용). 캠페인 발란스 무관.
- 2026-05-15 — **B-208.2 itch.io 카피 초안 박제** — 자율 작성 + 사용자 검수 대기. `docs/PM/release/itch-io-draft.md` 신규 (254 lines). 구성: §1 itch.io 폼 meta (URL slug / classification / kind / genre / tags 10개 / rating / pricing — `$0 minimum + pay-what-you-want suggested $3`) + §2 title `RL Dungeon` + §3 tagline (EN 44자 / KO) + §4 short desc (EN 175자 / KO) + §5 long desc EN+KO (Three pillars + Run structure 표 + How it plays + What this is not 명시 + Tech + Credits) + §6 embed options (1280×800, click-to-launch, fullscreen on, `index.html` 진입) + §7 cover image 후보 3안 + §8 스크린샷 후보 6장 + §9 출시 devlog 첫 글 초안 + §10 사용자 검수 체크리스트 8건 + §11 업로드 절차 9단계 (자체 플레이테스트 5회 통과 후 Public 전환). 영문 카피 톤 D-2026-05-15-15 Crisp/Functional 계승. 정체성 D-2026-05-12-4 한 줄 정체성 hero/sub/footnote 3분할 (landing 패턴 재사용). 메타 자기참조 0건 ([[feedback_no_meta_lessons]] 정합). **사용자 검수 대상**: URL slug (rld vs rl-dungeon) / 한국어 short desc 글자 수 / pay-what-you-want tip 금액 ($3 적정?) / 한국어 섹션 통합 vs 별 itch.io 페이지 분리.
- 2026-05-15 — **B-208 출시 페이지 1차 마감** — github pages 진입점용 마케팅 랜딩. **구조 결정 (D-2026-05-15-17)**: `web/index.html` = 랜딩 (신규), `web/play.html` = 게임 본체 (rename, history 보존). URL 패턴 `/` = 마케팅, `/play.html` = 실행. `web/css/landing.css` 신규 (200줄, `style.css` 와 분리 — 게임 UI 와 의존 격리). `web/js/i18n/dict-ko.js` + `dict-en.js`: `landing.*` 25키 추가 (meta / hero / concept 3 / screenshots / quickstart 5 / footnote / footer). 총 키 222 → 247 (양사전 동기 0 누락 검증). **HTML 콘텐츠**: 히어로 (logo + 정체성 카피 + 서브 + Play/GitHub CTA) → 핵심 컨셉 3 카드 (시드 런 / 알고리즘=캐릭터 / 시각적 RL 양념) → 스크린샷 2×2 placeholder (다음 사이클 캡처) → 빠른 시작 5 라인 → 푸터 (RL 교육은 부산물 / MIT). **메타**: `<meta name="description">` + `og:title` + `og:description` 동적 갱신 (data-i18n-content / data-i18n-attr-content 패턴, 토글 시 refreshMetaAttrs 호출). **버그 fix**: `.landing-body { display: flex; flex-direction: column }` 가 자식 `.landing-section` 의 max-width 와 cross-axis stretch 충돌로 content-min-width (149px) 로 collapse — `width: 100%` 명시로 해결 (`landing-hero` + `landing-section` 둘 다). **모바일 wrap 폴리시**: `word-break: keep-all` + `overflow-wrap: break-word` body 전역 + 600px 미디어 쿼리 컨셉 카드 padding 20 + 퀵스타트 폰트 0.92rem + 스크린샷 1-col + lang-toggle 좁힘. **README 갱신**: Phase 표 → M1~M5 milestone 표 (M5 진행 중 표기), 웹 실행 URL (` /` 랜딩 / `play.html` 게임) 분리 명시, 출시 페이지 / itch.io 호스팅 노트, 프로젝트 구조 (index.html / play.html / css/landing.css / i18n/), 향후 계획 (M5 → 1.0 / Act 2). **검증**: node --check dict 통과 + key parity 247=247 + Chrome headless 데스크탑 (1400×2200) 정상 + 모바일 (420×2400) 정상 (한국어 wrap 미세 정합) + play.html rename 후 캡처 정상 (title screen + 환영 토스트). 식별자 보존 (B-107). sim 무영향. **인계 잔여**: 스크린샷 4~6장 (B-208.1) / itch.io 페이지 카피 (B-208.2) / github pages 활성화 (B-208.3, admin 필요).
- 2026-05-15 — **한영 i18n 2차 마감** — 게임 모듈 텍스트 추출 (modifiers 12종 / tutorial 5메시지 / chapter 7개 / items 3종 / character desc+personality 15×2) + 언어 토글 UI (우측 컨트롤 상단, ko ↔ en, localStorage `rld_lang` 박제) + dict-en 1차 영어 카피 222키 (Crisp/Functional 톤). `web/js/i18n/dict-ko.js`: 222키 (149 + 73). `web/js/i18n/dict-en.js`: 222키 (누락 0). `web/js/game/tutorial.js`: STEPS 의 text 제거 + `t(\`tutorial.${id}\`)` + `t('tutorial.dismiss')` 도입 + i18n import. `web/js/game/briefing.js`: chapter label 에 `t(\`chapter.${n}\`)` 도입. `web/js/main.js`: `_renderModifierBand` / `selectCharacter` hire flow / character-desc 호출처 6곳 / ITEMS shop + 구매 메시지 / chapter 헤더 등 ~15곳 t() 치환. **언어 토글**: `web/index.html` 우측 컨트롤 상단 `#lang-toggle` 버튼, `web/css/style.css` `.lang-toggle` 스타일 (outline 톤, B-204 매칭), `web/js/main.js setupEventListeners` 의 토글 핸들러 (label 은 *다음 언어* 표시: ko 일 때 'EN', en 일 때 '한국어' / 클릭 시 setLang + updateUI + character-desc / modifier band / daily intro 갱신). **검증**: node --check (5/5 OK), dict 222키 (ko=en, 누락 0), 보간 작동 (guild.deaths_format / hire.confirm / daily.victory_msg / modifier.* / tutorial.* / chapter.* / item.* / character.desc.* 25종 케이스 ko↔en 양방향 작동). sim 무영향 (UI 전용).
- 2026-05-14 — **B-207 마감** — silent_q 모디파이어 활성 시 Q-heatmap / sparkline 자리에 명시 placeholder ("침묵의 학습 — 시각화 차단됨") 표시. D-4 정체성 보호 — RL 양념이 *사라진 게 아니라 모디파이어로 가려졌음* 을 사용자에게 명시. `web/index.html`: sparkline-wrap 다음에 `#silent-q-placeholder` div 추가 (디폴트 hidden, dashed 회색 테두리 + italic 보조 텍스트). `web/css/style.css`: `.silent-q-placeholder` 스타일 (sparkline-wrap 톤 매칭). `web/js/main.js _syncAgentModifiers`: muted 분기 끝에 placeholder display 토글. **sim 무영향** (sim 디렉토리에 silent_q / visualizationMuted 참조 없음). 검증: `ModifierSet(['silent_q']).visualizationMuted()` true, 빈/타 modifier false. M5 첫 마감 (M4 modifier 리뷰 I1 후속). 캠페인 게이트 재측정 불필요 (UI placeholder 전용).
- 2026-05-14 — **M4 모디파이어 12종 마감** (D-2026-05-14-14) — VISION §5 M4 의 12종 목표 충족. 환경 6 (slippery / heavy_fog / dim_torch / poison_floor / acid_rain / wind_gust) + 제약 6 (two_only / hp_cap_50 / mirror_input / no_heal / damage_boost / silent_q). 데일리 전용 유지 (D-9). 초기 명세의 food_drain / gold_dry 는 데일리 이코노미 격리 (D-8) 로 효과 0 → mirror_input / damage_boost 로 교체. `web/js/game/modifiers.js`: MODIFIERS 사전 12종 + ModifierSet 메소드 9개 (visibilityRange 확장 / shouldSkipTurn / poisonStepDamage / acidRainDamage / clampMaxHp / mirrorInput / healDisabled / damageMultiplier / visualizationMuted) + 신규 PRNG salt (SALT_WIND). `web/js/game/agent.js move()`: HEAL/TRAP/MONSTER 처리에 modifierSet 가드 (no_heal / damage_boost). `web/js/main.js handleAction`: wind_gust skipTurn + mirror_input + poison/acid 후처리. agent 생성 시 hp_cap_50 적용. `_syncAgentModifiers` 가 silent_q 시 showQValues=false + sparkline-wrap hide. **검증**: 12종 풀 확인 + pickModifiers 결정론 (seed 42 두 번 호출 동일 결과) + 캠페인 baseline 25.1/27 ✓ (영역 [23.5, 27.5] hit, modifier-off 무영향). **B-205 자연 closure** — 데일리 전용 유지로 slippery 30% 강도가 캠페인 발란스에 영향 줄 경로 없음.
- 2026-05-14 — **B-203 마감** — 세르파 누적 사망 한도 (D-4 verdict: Q-table 30% 노이즈 미채택, 학습 시각화 가치 보존). `web/js/game/run-state.js`: `DEATH_LIMIT = 4` 상수 export + `recordDeath()` 가 한도 도달 boolean 반환 + 신규 `resetForDeathLimit()` (fresh playthrough — runNumber/deathCount/clearedDungeons reset, ngPlusCount/bestTotalSteps 보존). `web/js/main.js triggerGameOver`: `this.deathLimitReached` 박제 + 게임오버 메시지·통계 보강. `startNewRun` 분기 — 한도 도달이면 resetForDeathLimit, 아니면 기존 startNewRun. `_updateGuildResources`: guild header 의 사망 카운터 (`#guild-deaths`) 갱신 + 임박 시 빨간색 강조. `web/index.html` guild header 에 `#guild-deaths` 신규. `web/css/style.css` `.guild-deaths`/`.guild-res-warn`. sim 무영향 검증 (sim 은 manual-play fail 만, HP 사망 trigger 안 함) — 3회 측정: 23.6 / 25.1 / 25.2 평균 ~24.6 (영역 [23.5, 27.5] hit). M4 P2 마지막 마감.
- 2026-05-14 — **B-202 마감** — 모바일 하단 탭 바 (Character / Dungeon / Train / Shop / Stats). `web/index.html` `<nav class="bottom-tabs">` (main 끝) 5개 button + 캐릭터/던전 섹션 신규 id (`#char-section`, `#dungeon-section`). `web/css/style.css`: 디폴트 hidden, 모바일 미디어 쿼리에서 `display: flex` + `position: fixed; bottom: 0` + `body { padding-bottom: 60px }` 가드 (B-201 minimap 영역 보호). `web/js/main.js setupEventListeners`: 클릭 시 `scrollIntoView({ behavior: 'smooth' })` + active class 토글. 데스크탑 무영향. **M4 모바일 트랙 (B-204 → B-201 → B-202) 완료**.
- 2026-05-14 — **B-201 마감** — 모바일 fit-to-screen + 미니맵 (큰 던전 ≥25). `web/css/style.css` 모바일 미디어 쿼리 (`@media (max-width: 700px)`) 안에 `.game-area { max-width: 100%; min-width: 0; box-sizing: border-box; }` + `#game-canvas { max-width: 100%; height: auto; }` — flex item 의 default `min-width: auto` 가 canvas intrinsic width 로 잡혀 viewport 넘던 버그 해결. `web/index.html` `#minimap-wrap` + `#minimap-canvas` 신규. `web/js/game/tilemap-renderer.js render()` 끝에 `this.onAfterRender?.()` hook 한 줄. `web/js/main.js` constructor 에서 minimap canvas/ctx 캐시 + onAfterRender 등록. `_renderMinimap()` 메소드: grid width/height < 25 면 wrap data-active="false" silent skip, ≥25 면 active="true" + canvas resize (1~5px/tile, max 120px) + 타일 채색 (WALL/GOAL/GOLD/MONSTER/TRAP/HEAL/START) + agent 흰색 강조점. 데스크탑 무영향, 작은 던전 (캠페인 대부분) 무영향. M4 모바일 트랙 (B-201 → B-202) 의 두 번째 마감. **B-202 인계**: 하단 탭 바를 `position: fixed; bottom: 0` 로 도입 시 minimap 가림 가능성 — fit/fixed overlay/접이식 중 진입 시점에 결정 필요.
- 2026-05-14 — **B-204 마감** — 모바일 핵심 스탯 3개 (HP / Gold / Steps) 만 디폴트 노출, 나머지 4개 (Run / Reward / Food / Clear Rate) 토글 접힘. `web/index.html` 의 7개 `.stat` 에 `data-priority="core|extra"` 추가 + `#stats-toggle` 더보기 버튼. `web/css/style.css` 모바일 미디어 쿼리 (`@media (max-width: 700px)`) 안에 `.stats:not(.expanded) .stat[data-priority="extra"] { display: none; }` + `.stats-toggle { display: inline-block; }`. `web/js/main.js setupEventListeners` 에 토글 핸들러 (`.expanded` class + aria-expanded + 더보기/접기 텍스트) + localStorage `rld_ui_stats_expanded` 박제로 페이지 새로고침 후에도 사용자 선호 유지. 데스크탑 (≥701px) 전체 노출 유지, 식별자 (id) 전부 보존 (B-107 정합).
- 2026-05-14 — **D-12 closure** — 17 알고리즘 파일 (qlearning, sarsa, sarsa-lambda, expected-sarsa, monte-carlo, dyna-q, double-qlearning, qv-learning, actor-critic, reinforce, tree-backup, prioritized-sweeping, acla, ensemble, local-qlearning, dqn, ppo) × 33 인스턴스 deflection 패치. `nextPos/nextKey/originalTile → intendedPos/intendedKey/intendedTile + actualKey` (agent.move() 직후 actualKey 재생성, hide 는 intended / track + restore 는 actual). agent.js 미수정. sim/simulator.js getGrid 에 `grid.modifierSet` 부착 활성화 (D-12 사유 주석 폐기). **측정 결과 (20-run HybridPlayer)**: off=25.6/27 ✓ (+1.1 vs B-109 24.5 — 경계 OOB 회피로 baseline 향상), heavy_fog=25.6/27 ✓, two_only=16.3/27, slippery=14.0/27 (-10.4 vs B-109 24.4), 3종 합산=15.2/27. **이중 페널티 가설 기각** (algorithm-only 14.6 vs 이중 14.0, +0.6 차이만) — 영향은 algorithm 측 deflection 자체. M3→M4 게이트 유지 ✓ (modifier-off 25.6/27 영역 hit). 후속: 캠페인 modifier 도입 시 slippery 30% 강도 (D-10) 재검토 필요.
- 2026-05-13 — **B-109 마감** — sim/simulator.js + sim/run-balance.js 에 modifier 통합. GameSimulator 옵션 `{modifierIds, modifierSeed}`, two_only 시 캐릭터 풀 픽 + pre-hire, manual-play 페널티 (heavy_fog ×1.25 humanSteps / slippery ×0.85 successRate), `estimateManualCost` 시그니처 확장으로 추정/실제 동기화. **측정 결과 (20-run HybridPlayer)**: off=24.5/27 ✓, slippery=24.4/27 ✓, heavy_fog=24.3/27 ✓ (모두 [23.5,27.5] hit), two_only=13.3/27 (D-9 데일리 전용으로 정당화), 3종=17.1/27. **slippery 는 manual-play approximation 만** — algorithm 15개 의 gold/monster restoration 이 intended vs actual position 분리 미흡한 pre-existing 버그로 deflection 시 OOB → D-12 박제. M3 → M4 게이트 통과.
- 2026-05-12 — **T2B-2 MVP 마감** (모디파이어 3종, 데일리 전용). `web/js/game/modifiers.js` (ModifierSet + pickModifiers, mulberry32 seeded). agent.js `_resolveAction` + `getVisibility` 격상. daily-mode.js `getDailyChallenge` 가 pickModifiers 호출. main.js: activeModifierSet 라이프사이클 + two_only 캐릭터 픽커 + 모디파이어 띠 + heavy_fog 시 fog 강제. 결정론 검증 (slip rate 0.300 over 10k, two_only seed=3 → ["dyna","doubleq"] reproducible, dailyChallenge 동일 UTC 일 idempotent). visual 검증 (heavy_fog 가시 2칸, slippery 11 push 중 3 deflection). 캠페인 회귀 sim (modifier off, 5×20-run): 24.6 / 25.6 / 24.9 / 25.4 / 24.1 — 모두 25.5/27 ± 2 영역 hit ✓.
- 2026-05-12 — **T2B-1 마감** (시드 기반 일일 챌린지 — 1.1 rng.js 분리 / 1.2 Daily 모드 + 데일리 던전 / 1.3 어제 비교 + 7일 캐러셀 / 1.4 B-110 흡수). 결정론 검증 통과 (시드 1656106231, 시작/목표 reproducible). Play 회귀 sim 25.0/27 ✓. agent-browser visual 검증 통과 (Daily 탭 → 도전 → 클리어/사망 → 어제 비교 메시지)
- 2026-05-12 — **T2B 본진 진입 인계 문서** 작성 (`handoffs/2026-05-12-act1-to-t2b.md`) — 다음 에이전트가 zero-base 진입 가능하도록 T2B-1/T2B-2 명세 + 검증 자산 + 자율 결정사항 + 위험 정리
- 2026-05-12 — **자율 진행 세션**: P1 8건 일괄 처리. B-108 식량 1→2 + sim 일관성 + 20-run baseline 회귀 (25.6/27 ✓), B-101 Q-Value 디폴트 ON + 숫자 제거, B-106 학습 성공률 sparkline 상시 표시, B-104 Ch.7 운영비 -30% + 보상 +30%, B-103 Ch.5 8→4 던전 축소 (Lv.20~23 컷, 31→27), T2B-3 잔여 (hire confirm + tutorial 한국어). agent-browser 통합 검증 통과
- 2026-05-12 — B-102/B-105/B-107 처리: PIT 빨간 X 분화, 캐릭터 카드 성격 태그 + 학명 hover 툴팁, 우측 컨트롤 10개 섹션 한국어 통일
- 2026-05-12 — B-004 Step-0 진입 모드: 진입 시 4개 패널만 노출 + canvas 힌트, 첫 클리어 시 일괄 펼침 + 토스트
- 2026-05-12 — B-001/B-002/B-003 일괄 처리: README / GDD / ACT1_SUMMARY 정체성 카피 통일, 가차 섹션 영구 삭제, 던전 마스터 모드 Act 2 후속 격하
- 2026-05-12 — 3축 병렬 비판 수행 (`critiques/`) + RL 포지셔닝 디베이트 → D-4 / D-5 확정 → VISION.md straw-man — **M1 마감**

## 블락된 항목

없음. **M4 진입 — D-12 정리 완료**. 알고리즘 17개 deflection 패치 적용, sim 측 grid.modifierSet 활성화. 캠페인 modifier 도입 (모디파이어 12종 디자인 시점) 전에 modifier.slippery 30% 강도 (D-10) 재검토 필요 — algorithm 측 자연 영향 -10.4 가 캠페인 발란스 25.5/27 ± 2 영역 보호와 충돌.

## 핵심 지표 (D-12 closure 후, 2026-05-14)

- HybridPlayer modifier off (20-run): **25.6/27 ✓** (D-4 의 25.5 ± 2 정확 hit, D-12 패치로 +1.1 향상)
  - 이전 (B-109): 24.5/27 ✓ (5×20 historical: 24.6 / 25.6 / 24.9 / 25.4 / 24.1)
- HybridPlayer modifier ON — **데일리 전용 측정** (D-9, 캠페인 게이트 미적용), algorithm-side deflection 활성:
  - slippery 단독: **14.0/27 ❌** (algorithm 측 deflection 자연 영향 -10.4, 캠페인 도입 전 강도 재검토 필요)
  - heavy_fog 단독: **25.6/27 ✓** (movement 미영향)
  - two_only 단독: **16.3/27** (D-9 데일리 전용으로 정당화, 캠페인 미적용)
  - 3종 합산: **15.2/27** (데일리 전용 스택 — 영역 이탈 의도됨)
- 던전 수: 31 → **27** (B-103 Ch.5 축소, Lv.20~23 은 grid.js 보존 자산)
- 우측 컨트롤 10개 섹션 한국어, 튜토리얼 5개 메시지 한국어
- 첫 진입 — Step-0 4개 패널 + canvas 힌트 + Q-Value 디폴트 ON + sparkline placeholder
- **데일리 모드**: Daily 탭 + 시드 결정론 + 20×20 PCG 던전 + 어제 비교 + 7일 캐러셀, localStorage `rld_daily_history` 별도 키 (NG+ 영향 X)
- **모디파이어 3종** (데일리 전용): slippery (30% 빗나감, seeded), two_only (14-character pool — scout 제외 — 에서 2명 시드 픽), heavy_fog (시야 5→3 칸). 모디파이어 띠 + 데일리 패널 칩 UI
- **sim 측 modifier 통합**: `--modifier=slippery,heavy_fog,two_only` `--seed=N` CLI 플래그. manual-play approximation (slippery/heavy_fog) + two_only pre-hire

## 다음 마일스톤 (VISION §5 동기화)

- **M1** ✅ — 그림 그리기 (이 PM 디렉토리 + VISION.md)
- **M2** ✅ — P0 4건 → *"정체성 정리 완료"*
- **M3** ✅ — T2B 본진 (시드 + 모디파이어 MVP 3 + 알고리즘=캐릭터) + B-108/109 → ***alpha***
- **M4** ✅ — D-12 closure + 모바일 트랙 + 사망 페널티 + 모디파이어 12종 → ***beta***
- **M5** (~6~7개월) — 폴리시 (B-207 ✅) + 한영 i18n + 출시 페이지 + BGM 4트랙 → ***1.0*** **← 진입**
